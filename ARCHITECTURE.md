# tap-web — Architecture

> **⚠️ SCAFFOLD STATE**: This repo currently contains only Sentry configuration files (`instrumentation.ts`, `sentry.client.config.ts`, `sentry.server.config.ts`) and `.env.example`. The architecture described below is the target implementation blueprint.

## Overview
Single-page promotional website built with Next.js 14 App Router. Optimized for fast load times, SEO, and mobile-first experience.

## Folder structure

**Current state:**
```
tap-web/
├── instrumentation.ts          # ✅ Sentry initialization hook
├── sentry.client.config.ts     # ✅ Client-side Sentry config
├── sentry.server.config.ts     # ✅ Server-side Sentry config
├── .env.example                # ✅ Environment variable template
├── CLAUDE.md                   # ✅ AI context documentation
├── ARCHITECTURE.md             # ✅ This file
└── README.md                   # ✅ User-facing documentation
```

**Target structure (to be implemented):**
```
tap-web/
├── .github/
│   └── workflows/
│       └── ci.yml              # GitHub Actions: lint + type-check + test
├── public/
│   ├── images/                 # product photos, hero images
│   └── fonts/                  # custom web fonts (if any)
├── src/
│   ├── app/
│   │   ├── layout.tsx          # root layout: <html>, metadata, global styles
│   │   ├── page.tsx            # landing page (/) — hero + product showcase
│   │   ├── globals.css         # Tailwind directives, CSS variables
│   │   └── favicon.ico
│   ├── components/
│   │   ├── Hero.tsx            # hero section with CTA
│   │   ├── ProductShowcase.tsx # shirt images, description
│   │   ├── Footer.tsx          # contact, social links
│   │   └── ui/                 # shared UI primitives (Button, etc.)
│   └── lib/
│       └── utils.ts            # helper functions (cn for classnames, etc.)
├── instrumentation.ts          # ✅ Next.js instrumentation hook → Sentry init
├── sentry.client.config.ts     # ✅ Sentry client-side config
├── sentry.server.config.ts     # ✅ Sentry server-side config
├── tailwind.config.ts          # Tailwind theme, custom colors
├── tsconfig.json
├── next.config.js              # Next.js config (images, Sentry plugin)
├── .env.example                # ✅
├── .env.local                  # git-ignored, local secrets
├── package.json
└── README.md                   # ✅
```

## Request lifecycle

### Initial page load (`GET /`)
1. **Next.js runtime** loads `instrumentation.ts` → Sentry initialized
2. **Server** renders `src/app/page.tsx` (Server Component by default)
3. **HTML sent** to browser with inlined critical CSS
4. **Client hydration** React takes over interactivity
5. **Images lazy-load** via `next/image` (optimized WebP/AVIF)

### Subsequent interactions
- All navigation client-side (single-page app)
- Any dynamic data fetches (if added) happen in Server Components or API routes

## Data model
No backend or database — static content only.

**Product data** (if managing multiple shirts):
- Hardcoded in `src/app/page.tsx` or
- JSON file in `public/data/products.json` fetched at build time

Future: connect to CMS (Contentful, Sanity) for non-technical updates.

## Component patterns

### Server Components (default)
- `src/app/page.tsx` — static, SEO-friendly
- `src/components/ProductShowcase.tsx` — no interactivity needed

### Client Components (opt-in with `"use client"`)
- `src/components/Hero.tsx` — if CTA button has click tracking
- Any component using `useState`, `useEffect`, event handlers

### Styling
- **Tailwind utility classes** for 90% of styles
- **CSS variables** in `globals.css` for brand colors, spacing scale
- **Dark mode**: optional, add `dark:` variants if needed

## Performance
- **Core Web Vitals targets**:
  - LCP < 2.5s
  - FID < 100ms
  - CLS < 0.1
- **Image optimization**: `next/image` auto-generates srcsets, lazy-loads below fold
- **Font loading**: `next/font` with `display=swap`

## SEO
- **Metadata** in `src/app/layout.tsx`:
  - `<title>` and `<meta name="description">`
  - Open Graph tags for social sharing
  - Twitter Card tags
- **Sitemap**: auto-generated by Next.js at `/sitemap.xml` (add `sitemap.ts` if custom routes)
- **Structured data**: add JSON-LD for Product schema if needed

## Error handling
- **Sentry** captures all uncaught errors (client + server)
- **Custom error boundaries**: add `error.tsx` in `src/app/` if needed
- **404 page**: add `not-found.tsx` for custom 404 design

## Deployment
- **Vercel** (recommended): auto-detects Next.js, zero config
- **Environment variables** set in Vercel dashboard:
  - `SENTRY_DSN` (required)
  - `NEXT_PUBLIC_SITE_URL` (optional, for canonical URLs)
- **Build command**: `npm run build`
- **Output**: static + serverless functions (App Router pages)

## Observability
- **Sentry**: error tracking, performance monitoring
- **Vercel Analytics**: Core Web Vitals, real user metrics
- **Logs**: view in Vercel Functions tab (if using server-side features)

## Security
- No user authentication or sensitive data
- **Content Security Policy**: add to `next.config.js` if embedding third-party scripts
- **HTTPS**: enforced by Vercel

## Future enhancements
- Add CMS for product content (Sanity, Contentful)
- E-commerce integration (Stripe, Shopify)
- A/B testing (Vercel Edge Middleware + feature flags)
- Analytics (Google Analytics, Plausible)
